flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/logo.png"
  android_12:
    color: "#FFFFFF"
    image: "assets/logo.png"
  web: false # Since web usually loads fast or has different splash logic, but we can enable if native look desired. Prompt said config.
